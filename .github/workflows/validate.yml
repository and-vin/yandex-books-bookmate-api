name: Validate OpenAPI spec

on:
  push:
    branches: [main]
    paths:
      - "openapi.yaml"
      - "scripts/check_openapi_consistency.py"
      - ".github/workflows/validate.yml"
  pull_request:
    paths:
      - "openapi.yaml"
      - "scripts/check_openapi_consistency.py"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - run: pip install openapi-spec-validator pyyaml
      - run: python -m openapi_spec_validator openapi.yaml
      - run: python scripts/check_openapi_consistency.py openapi.yaml
